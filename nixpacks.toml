[phases.setup]
nixPkgs = ["python311Full", "gcc", "dejavu_fonts"]
aptPkgs = ["python3-pip"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT"
